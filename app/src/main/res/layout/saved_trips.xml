<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_all"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/foot"
        android:layout_alignParentTop="true"
        android:orientation="vertical">

        <TextView
            android:id="@+id/total_per_user"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_horizontal"
            android:text="Saved Trips"
            android:textColor="@color/orange_footer_head"
            android:textSize="22sp"
            android:visibility="gone" />

        <ListView
            android:id="@+id/saved_trips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:divider="#9b9999"
            android:dividerHeight="0.1dp"
            android:listSelector="@drawable/list_selector_common"></ListView>
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/foot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/orange_footer_head"
        android:gravity="center_horizontal">

        <ImageButton
            android:id="@+id/add_new_trip"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_margin="5dp"
            android:background="@drawable/add_action" />
    </RelativeLayout>


</RelativeLayout>